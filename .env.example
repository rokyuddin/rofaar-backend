# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL via postgres.js)
DATABASE_URL=postgresql://user:password@localhost:5432/rofaar

# JWT
JWT_SECRET=change_me_to_a_long_random_string_at_least_32_chars
JWT_EXPIRES_IN=7d
